{
  "$id": "gts://gts.x.core.iam.settings_category.v1~",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "IAM Settings Category",
  "description": "A category of IAM security settings. Well-known instances identify each category (e.g. password-policy, mfa, session, lockout).",
  "type": "object",
  "required": ["id", "name"],
  "properties": {
    "id": {
      "description": "GTS well-known instance identifier for this category.",
      "type": "string"
    },
    "name": {
      "description": "Human-readable name of the category.",
      "type": "string"
    },
    "description": {
      "description": "Human-readable description of the category.",
      "type": "string"
    }
  },
  "additionalProperties": false
}
